export enum ClaimStatus {
    PENDING = 0,        // CLAIM_STATUS_PENDING(0)
    ACCEPTED,           // CLAIM_STATUS_ACCEPTED(1)
    REJECTED,           // CLAIM_STATUS_REJECTED(2)
}

export enum CareerType {
    ENC_VC = 0,         // CAREER_TYPE_ENC_VC(0)
    IPFS_HASH,          // CAREER_TYPE_IPFS_HASH(1)
}

export enum UserType {
    EMPLOYER = 0,       // USER_TYPE_EMPLOYER(0)
    EMPLOYEE,           // USER_TYPE_EMPLOYEE(1)
}
